class: SmartyResource
mimetype: text

{if $resource->test neq ''}{$resource->test_message}\n\n{/if}{if $resource->cash eq 'Y'}$$$ CASH ORDER $$$\n$$$ CASH ORDER $$$\n$$$ CASH ORDER $$$\n\n{/if}Order For: {$resource->first_name}\nOrder ID: {$resource->order_id}{if $resource->order_type eq 'D'}{assign var=delivery_location_info value=$resource->delivery_info}\nDelivery To: {$delivery_location_info->name}\nTime: {$resource->requested_delivery_time}\n{$delivery_location_info->address1}{if $delivery_location_info->address2 neq ''}\n{$delivery_location_info->address2}{/if}\n{$delivery_location_info->city}, {$delivery_location_info->state}\nPhone: {$delivery_location_info->phone_no}{else}\nPickup: {$resource->pickup_date} {$resource->pickup_time}\n{/if}\nItems:  {$resource->order_qty}{if $resource->note neq ''}\n\nNote: {$resource->note}\n{/if}{foreach from=$resource->order_details key=index item=menu_item}{assign var=item_name value=$menu_item.item_print_name}{assign var=item_size value=$menu_item.size_print_name}{assign var=item_quant value=$menu_item.quantity}{assign var=item_price value=$menu_item.item_total_w_mods}\n \n{if $item_quant neq '1'}{$item_quant} {/if}{if $item_size|lower neq 'one size'}{$item_size} {/if}{$item_name} {$resource->order_identifier}\n{if $menu_item.note neq ''}\nNote: {$menu_item.note|escape}\n \n{/if}{if $menu_item.order_detail_modifiers|@count > 0}\nWITH: {foreach from=$menu_item.order_detail_modifiers key=index2 item=modifier_record name=no_mod}{assign var=mod_item_name value=$modifier_record.mod_print_name}\n--{$mod_item_name}{if $modifier_record.mod_quantity > 1}(x{$modifier_record.mod_quantity}){/if}{if $modifier_record.mod_total_price neq '0.00'} ${$modifier_record.mod_total_price|substr:0:-1}{/if}{/foreach}\n{/if}{if $menu_item.order_detail_sides|@count > 0}\nSIDE OF: {foreach from=$menu_item.order_detail_sides key=index2 item=modifier_record name=side}{assign var=mod_item_name value=$modifier_record.mod_print_name}\n--{$mod_item_name}{if $modifier_record.mod_quantity > 1}(x{$modifier_record.mod_quantity}){/if}{if $modifier_record.mod_total_price neq '0.00'} ${$modifier_record.mod_total_price|substr:0:-1}{/if}{/foreach}\n{/if}\n    ------{/foreach}\n   Sub: {$resource->order_amt}\n   Tax: {$resource->total_tax_amt}\n{if $resource->promo_amt neq '0.00'} Promo: {$resource->promo_amt}\n{/if}{if $resource->delivery_amt neq '0.00'}Dlvry: {$resource->delivery_amt}\n{/if}\n Total: {$resource->grand_total_to_merchant}\n \n{if $resource->cash eq 'Y'}$$$ CASH ORDER $$$\n$$$ CASH ORDER $$$\n$$$ CASH ORDER $$$\n{else}  Tip: {$resource->tip_amt}\n{/if}\n